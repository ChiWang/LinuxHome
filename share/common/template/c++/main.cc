#include <iostream>
#ifdef DEBUG
#include <assert.h>
#endif
//#include <boost/program_options.hpp>
//---------------------------------------------------
int main(int argc, char* argv[]){
/*
//  ------> Program_options
  boost::program_options::options_description options("Usage: MyPro [options] \nOptions");

  options.add_options()
    ("help,h","Use -h or --help to display this information")
    ("version,v","display version information")
    ("filename",boost::program_options::value<std::string>(),"to find file");

  boost::program_options::variables_map vmap;
  boost::program_options::store(parse_command_line(argc,argv,options),vmap);
  boost::program_options::notify(vmap);

  if (vmap.count("help")) {
    std::cout<<options<<std::endl;
    return 0;
  } else if (vmap.count("version")) {
    std::cout<<"MyPro Version = 0.21.8"<<std::endl;
  }
*/

  return 0;
}

